import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto-moviles-bi',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
